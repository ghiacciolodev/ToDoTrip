targets:
  $default:
    builders:
      json_serializable:
        options:
          # The API speaks snake_case, Dart speaks camelCase. Configuring it
          # once here avoids a @JsonKey annotation on every single field.
          field_rename: snake