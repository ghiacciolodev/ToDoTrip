# Brand assets

| File | What it is | Status |
|---|---|---|
| `wordmark.png` | The "ToDoTrip" lettering, teal on a transparent background. Shown at the top of the repository README. | in the repository |
| `icon.png` | The 512×512 app icon: a white T on the brand teal. | not here yet |

## The app icon

`icon.png` is what a launcher icon would be generated from. The app still ships
Flutter's default; replacing it needs the `flutter_launcher_icons` package and
one entry in `pubspec.yaml` — a dependency nobody has agreed to yet.
